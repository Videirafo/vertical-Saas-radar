# Security

Do not report sensitive vulnerabilities through a public Issue.

This repository intentionally avoids credentials and private data in its default configuration. Never commit API keys, access tokens, private URLs, customer records or authenticated page content.

The public web demo reads generated local JSON files and does not provide mutation endpoints in v0.1.
